# Core — enough to run notebooks/three_flow_lab.ipynb and tests/test_lab.py
# (the notebook reads committed artifacts/; no private data needed)
numpy>=2.0
pandas>=2.2
pyarrow>=16
torch>=2.3
scikit-learn>=1.5
matplotlib>=3.9
jupyter
ipykernel

# --- Optional: only needed to RE-RUN the original research pipelines ---
# flow1 s3_events.py (English SVO extraction):
#   spacy  +  python -m spacy download en_core_web_sm
# flow2 t1_embed.py / t2_sentiment.py (FinBERT embeddings/sentiment):
#   transformers
# flow3 KR scripts (Korean tokenizing / KRX prices):
#   kiwipiepy
#   pykrx
